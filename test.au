module system = import auro.system;
module string_mod = import auro.string;

type string = string_mod.string;

import test.extra;

module array_mod $<> = import auro.array;
type array = array_mod.array;
void insert (string) = array_mod.insert;

module exported {
  sys = system;
  str = string;
  str_m = string_mod;
  cat = cat;
  bar = extra_f;

  //arr = array;
  //arr = string$<T=int>;
  //arr = system$<T=int>;
  arr = array$<T=string>;
  
  //ins = insert$<T=string>;
}

void cat (string, string) = string_mod.concat;

export exported;